name: Notify Telegram on Deploy

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Debug raw API response
        run: |
          sleep 60

          RESPONSE=$(curl -s "https://api.vercel.com/v6/deployments?projectId=${{ secrets.VERCEL_PROJECT_ID }}&limit=1" \
            -H "Authorization: Bearer ${{ secrets.VERCEL_TOKEN }}")

          DEPLOY_ID=$(echo $RESPONSE | python3 -c "import sys,json; d=json.load(sys.stdin)['deployments'][0]; print(d['uid'])")

          echo "=== DEPLOY ID ==="
          echo $DEPLOY_ID

          echo "=== RAW COMPLETO ==="
          curl -s "https://api.vercel.com/v2/deployments/${DEPLOY_ID}/events" \
            -H "Authorization: Bearer ${{ secrets.VERCEL_TOKEN }}" | head -50
